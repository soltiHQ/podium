package visual

// SectionLabel is the standard class for small uppercase labels
const SectionLabel = "text-[11px] uppercase tracking-[0.05em] text-muted"

// SectionTitle is the standard class for prominent section headings
const SectionTitle = "text-xs font-semibold uppercase tracking-wider text-muted-strong"
